package UI;

import javax.swing.*;

public class UserUI {
    public UserUI() {

        JFrame frame = new JFrame("User - UI");

        frame.setSize(1500,750);
        frame.setLayout(null);
        frame.setVisible(true);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setLocationRelativeTo(null);
    }
}
